@AGENTS.md

All project instructions live in `AGENTS.md` (imported above). Roles are available as subagents in `.claude/agents/`, launchers as slash commands in `.claude/commands/`, and shared skills in `.claude/skills/`; all three are symlinks into `.agents/`.
