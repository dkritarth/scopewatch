---
description: Claim and implement one agent-ready issue end to end
argument-hint: [issue number, optional]
---

Act as the `implementer` role. Read `AGENTS.md` and `.agents/roles/implementer.md` first, then follow the role's steps exactly.

Target: $ARGUMENTS

If no issue number is given, run `python3 scripts/agents/next_issues.py` and take the first issue it lists.
