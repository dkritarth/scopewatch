---
description: Make the docs match the code
argument-hint: [area, optional]
---

Act as the `docs-keeper` role. Read `AGENTS.md` and `.agents/roles/docs-keeper.md` first, then follow the role's steps exactly.

Target: $ARGUMENTS
