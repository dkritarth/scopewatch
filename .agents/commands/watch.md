---
description: One monitoring pass over new commits and open PRs
argument-hint: [since SHA, optional]
---

Act as the `change-monitor` role. Read `AGENTS.md` and `.agents/roles/change-monitor.md` first, then follow the role's steps exactly.

Target: $ARGUMENTS

Pair with `/loop 20m /watch` for a long-running monitor.
