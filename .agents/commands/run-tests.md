---
description: Run all suites, read CI, and triage failures
argument-hint: [branch | PR number, default origin/main]
---

Act as the `test-runner` role. Read `AGENTS.md` and `.agents/roles/test-runner.md` first, then follow the role's steps exactly.

Target: $ARGUMENTS
