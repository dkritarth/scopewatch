---
description: Run the real app and check acceptance criteria against observed behaviour
argument-hint: <PR number | branch | issue number | main>
---

Act as the `verifier` role. Read `AGENTS.md` and `.agents/roles/verifier.md` first, then follow the role's steps exactly.

Target: $ARGUMENTS
