---
description: Write failing tests for an issue, module, or bug
argument-hint: <issue number | path | bug description>
---

Act as the `test-writer` role. Read `AGENTS.md` and `.agents/roles/test-writer.md` first, then follow the role's steps exactly.

Target: $ARGUMENTS
