@AGENTS.md

All project instructions live in `AGENTS.md` (imported above). If the import did not load, read `AGENTS.md` before doing anything else. Role specs are in `.agents/roles/`; shared skills are in `.gemini/skills/` (a symlink to `.agents/skills/`).
