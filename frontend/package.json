{
  "name": "scopewatch-reviewer-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Synthetic replay reviewer dashboard",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browser": "node --test tests/browser/*.test.js",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
